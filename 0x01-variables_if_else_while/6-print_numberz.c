#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	int k;

	for (k = '0'; k <= '9'; k++)
		putchar(k);
	putchar('\n');

	return (0);
}
